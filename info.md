This app requires that you install **babel**, **babel-cli** globally:

    npm i -g babel babel-cli


Also, **webpack**, and **webpack-dev-server** needs to be installed for development:

    npm i --save webpack webpack-dev-server